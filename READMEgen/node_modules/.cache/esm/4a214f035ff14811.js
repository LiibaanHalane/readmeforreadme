let inquirer,writeFile,generateMarkdown;_5ff‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_5ff‍.w("fs/promises",[["writeFile",["writeFile"],function(v){writeFile=v}]]);_5ff‍.w("./utils/generateMarkdown.js",[["default",["generateMarkdown"],function(v){generateMarkdown=v}]]);


//import instead of require because we are using ES modules

const questions = [
  {
    type: 'input',
    name: 'title',
    message: 'What is your project title?'
  },
  {
    type: 'input',
    name: 'description',
    message: 'Provide a description of your project:'
  },
  // Add more questions for the remaining sections
];

function writeToFile(fileName, data) {
  fs.writeFileSync(fileName, data);
}

function init() {
  inquirer.prompt(questions)
    .then(answers => {
      const markdown = generateMarkdown(answers);
      writeToFile('README.md', markdown);
    });
}

init();